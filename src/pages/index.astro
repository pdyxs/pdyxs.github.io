---
// `/` is a thin shortcut that renders the home lens, identical to /lens/home —
// the bespoke front page is gone; the home lens (LensStackCard in page mode) is
// the front page now.
import LensPage from '@components/lens/LensPage.astro';
---
<LensPage name="home" />
