---
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const tags = await getCollection('tag');
  return tags.map(t => ({
    params: { slug: t.id },
  }));
}

const { slug } = Astro.params;
return Astro.redirect(`/card/tag/${slug}`, 301);
---
