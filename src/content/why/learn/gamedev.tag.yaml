# Authored — tag a card with `why/learn/gamedev` in its frontmatter.
# Curated in issue #87; 8 cards carry it. (It was `why/learn/game-development`
# until the value was renamed — the declaration, the cards and the test all
# moved together, since filterVisibleNodes keys on the declared name.)
name: Game development
description: >
  Write-ups with something transferable in them about making games.
