---
interface Props {
  activeUid?: string;
}
const { activeUid } = Astro.props;
import CardStack from './CardStack.svelte';
const activeHtml = Astro.slots.has('default') ? await Astro.slots.render('default') : null;
---

<CardStack client:load {activeUid} {activeHtml} />
