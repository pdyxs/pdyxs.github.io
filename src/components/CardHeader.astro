---
interface Props {
  title: string;
  titleSuffix?: string;
}
const { title, titleSuffix } = Astro.props;
---
<div class="card-header">
  <span class="card-header-title"><b>{title}</b>{titleSuffix}</span>
  <slot />
</div>
